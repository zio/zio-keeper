apiVersion: v1
kind: Service
metadata:
  name: {{ include "experiment-chart.fullname" . }}
  labels:
    {{- include "experiment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "experiment-chart.selectorLabels" . | nindent 4 }}
